image:
  tag: "0.3.0"
  pullPolicy: Never       # local image loaded via minikube — no registry pull

replicaCount: 1

config:
  thunderInsecureTls: "true"

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 250m
    memory: 256Mi